# 视频规则笔记（2026-02-25）

来源：
- `https://www.bilibili.com/video/BV1Lb411z7bB/`
- `https://www.bilibili.com/video/BV1jb411z7FQ/`

处理方式：
- 两段视频完整音频转写（Whisper-1）。
- 长视频按 10 分钟切片共 11 段，已合并为统一时间轴。
- 证据文件：
  - `output/transcripts/BV1Lb411z7bB.whisper1.timeline.txt`
  - `output/transcripts/BV1jb411z7FQ.whisper1.timeline.txt`

## 高置信规则（已用于实现）

1. 开局是“三手布局”，并且布局时双方不可见。
- 证据：`BV1Lb411z7bB` 时间段约 `0-4s`、`341-345s`。

2. 三手若同点重叠，该点转减点。
- 证据：`BV1Lb411z7bB` 时间段约 `357-363s`，原话包含“重复点就变成减点”“减去五目棋”。

3. 争先手由贴目决定，不是固定黑先。
- 证据：`BV1Lb411z7bB` 时间段约 `3-30s`、`374-378s`；`BV1jb411z7FQ` 时间段约 `471-480s`。

4. 减点价值为 5 目（落上去会亏 5）。
- 证据：`BV1Lb411z7bB` 时间段约 `362-363s`、`1513-1516s`。

5. 加点价值为 5 目，多个加点累计可形成明显分差。
- 证据：`BV1Lb411z7bB` 时间段约 `1479-1482s`（“加点...20目棋”）；`BV1jb411z7FQ` 时间段约 `1161-1164s`（“最后一个加点加五目棋”）。

6. 隐藏手是核心机制，查找/搜索会围绕隐藏手展开。
- 证据：两段视频大量片段。

7. 基地子存在“5目子”语义。
- 证据：`BV1jb411z7FQ` 时间段约 `1765-1768s`（“五目的子”）。

## 当前实现对应

- 减点：重叠动态减点，首次落子 `-5`。
- 加/减点默认位置：
  - 减点：`(2,2) (2,8) (8,2) (8,8)`
  - 加点：`(0,5) (5,0) (10,5) (5,10)`
- 基地子：存活总值 5（活子 1 + 额外 4）；被提时提子方即时 `+5`。
- 棋盘显示：基地子特殊标记、最后一步高亮。
- 隐藏子交互：先选点再提交，提交后对双方不可见，触发显露条件后公开。

## 仍待你最终确认的点

1. 认输是否固定按“输 20 目”计（视频解说提到比赛计分口径，但可能是赛事小分规则而非对局基本规则）。
2. “查找不消耗落子回合”在视频中有间接迹象，但建议你按你要复刻的版本再确认一次。

